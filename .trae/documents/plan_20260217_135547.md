# 架构持续优化计划

## 🔴 高优先级 - 立即处理

### 1. 解决类名冲突
**问题**: 存在重复定义的类，可能导致混淆
- `SymbolTracker`: `src/wiki/incremental/symbol-tracker.ts` vs `src/wiki/location/symbol-tracker.ts`
- `ArchitectureDiagramGenerator`: `src/diagram/architecture-diagram.ts` vs `src/wiki/diagram/architecture-diagram.ts`
- `DependencyGraph`: `src/wiki/graph/dependency-graph.ts` vs `src/architecture/dependency-graph.ts`

**方案**: 合并或重命名区分，统一到单一模块

### 2. 提取公共工具方法
**问题**: 多处重复的工具方法
- `extractSections` - 章节提取逻辑
- `groupSymbolsByKind` - 符号分组逻辑
- `getModuleName` - 模块名称提取

**方案**: 创建 `src/utils/content-utils.ts` 统一实现

### 3. 添加缺失的单元测试
**缺失测试的关键模块**:
- `WikiMemory` - 核心功能
- `WikiContextCompressor` - 核心功能
- `WikiAutoSync` - 核心功能

---

## 🟡 中优先级 - 近期处理

### 4. 拆分 CLI 模块
**问题**: `src/cli/index.ts` 1187 行，过于庞大
**方案**: 按命令拆分为独立模块
```
src/cli/
├── index.ts (入口)
├── commands/
│   ├── generate.ts
│   ├── parse.ts
│   └── wiki/
│       ├── init.ts
│       ├── generate.ts
│       └── ...
```

### 5. 替换 any 类型
**问题**: CLI 中 74 处 `any` 类型
**方案**: 定义明确的接口类型 `CLIOptions`

### 6. 异步文件操作优化
**问题**: 多处使用同步文件操作 `fs.readFileSync`
**方案**: 改用 `fs/promises` API

---

## 🟢 低优先级 - 后续优化

### 7. 统一日志框架
**问题**: 生产代码中使用 `console.log/warn`
**方案**: 创建统一的 Logger 模块

### 8. 优化循环计算
**问题**: 多次遍历相同数组
**方案**: 合并为单次遍历

### 9. 清理无用代码
**问题**: 空实现方法如 `applyConvention`
**方案**: 删除或实现功能

---

## 📊 预计工作量

| 优先级 | 任务数 | 预计时间 |
|--------|--------|----------|
| 高 | 3 | 1-2 天 |
| 中 | 3 | 2-3 天 |
| 低 | 3 | 1 天 |

**总计**: 9 个任务，约 4-6 天